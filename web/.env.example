# Local `next dev` ignores this file unless you copy it to .env.local.
# On Vercel, set NEXT_PUBLIC_API_URL on the web project, then rebuild.

# Development (default in code if unset):
# NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Production:
NEXT_PUBLIC_API_URL=https://api-blond-delta.vercel.app/api
